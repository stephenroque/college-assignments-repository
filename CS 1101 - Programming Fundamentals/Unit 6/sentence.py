# Assume we have a sentence
sentence = "All men are created equal, then a few become firemen."

# Convert the sentence into a word list
wordList = sentence.split()

# Reverse the word list
reversedWordList = wordList[::-1]

print("Word List: " + str(wordList))
print("Reversed Word List: " + str(reversedWordList))

